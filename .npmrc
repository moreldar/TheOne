# pnpm 10+'s supply-chain safety check refuses to install packages
# published within a rolling window (minimum-release-age), as a defense
# against a just-published-malicious-version attack. That's a sensible
# default for CI/production, but it also blocks perfectly normal installs
# of a fast-moving lockfile in local dev, so it's disabled here.
minimum-release-age=0
